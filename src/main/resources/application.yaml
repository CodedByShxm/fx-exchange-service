spring:
  application:
    name: fx-exchange-service
  datasource:
    url: jdbc:postgresql://
    username: postgres
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

fx:
  api:
    key: