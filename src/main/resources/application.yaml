spring:
  application:
    name: fx-exchange-service
  datasource:
    url: 
    username: postgres
    password: !
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus

server:
  port: 8080

fx:
  api:
    key: